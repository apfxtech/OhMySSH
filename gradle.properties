kotlin.code.style=official
kotlin.daemon.jvmargs=-Xmx3072M

#Gradle
org.gradle.jvmargs=-Xmx4096M -Dfile.encoding=UTF-8
org.gradle.caching=true

#Android
android.useAndroidX=true
android.nonTransitiveRClass=true

#App
# Version of record. The release workflow overrides all three from the git tag
# and syncs the first two back here after publishing. The major must stay at 1
# or above: jpackage rejects a macOS bundle version that starts with 0.
ohmysshVersionName=1.1.2
ohmysshVersionCode=1001002
ohmysshReleaseTag=
